#pragma once

class Input
{
public:
    Input();
    virtual ~Input();

    // TODO: connectable signal pressed()
    //       joypad and keyboard independence
    //       possibility to set input device
};
